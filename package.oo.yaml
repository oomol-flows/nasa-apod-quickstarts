version: 0.0.4
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  oomol-file: 0.0.20
  oomol-preview: 0.1.7
  llm: 0.3.5
icon: ":twemoji:globe-showing-asia-australia:currentColor:"
name: nasa-apod-quickstarts
description: Examples in quickstarts
displayName: NASA APOD
repository: https://github.com/oomol-flows/nasa-apod-quickstarts
is_template: true
